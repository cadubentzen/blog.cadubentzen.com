<!DOCTYPE html>
<html lang="en-US">
  <head>
    {% include "head.liquid" %}
  </head>
  <body>
    <header>
      {% include "header.liquid" %}
    </header>
    <main>
      <article>
        <h1>{{ page.title }}</h1>
        <h4>
          {% if page.published_date %}
            {{ page.published_date | split: " " | first }}
          {% endif %}
        </h4>
        {{ page.content }}
      </article>
    </main>
    <footer>
      {% include "footer.liquid" %}
    </footer>
  </body>
</html>
